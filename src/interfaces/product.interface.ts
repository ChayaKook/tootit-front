export interface ProductModel {
    id?: string;
    code: string;
    name: string;
    description: string;
    image: string;
    price: number;
    category: string;
    inventoryStatus: string;
}